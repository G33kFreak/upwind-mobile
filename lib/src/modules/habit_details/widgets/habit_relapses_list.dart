import 'package:flutter/material.dart';

class HabitRelapsesList extends StatelessWidget {
  const HabitRelapsesList({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
